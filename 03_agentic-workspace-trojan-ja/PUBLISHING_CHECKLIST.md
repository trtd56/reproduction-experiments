# Publishing Checklist

- [x] Include deterministic toy reproduction script.
- [x] Include Claude Code demo script with harmless marker payload.
- [x] Include harness/model matrix script.
- [x] Include pytest tests for deterministic behavior.
- [x] Include sanitized aggregate metrics used by the article.
- [x] Exclude raw sandbox directories.
- [x] Exclude `__pycache__` and generated bytecode.
- [x] Exclude credentials, `.env` files, API keys, and private setup logs.
- [x] Exclude paper PDFs and cloned upstream repositories.
- [x] Exclude full CLI stdout/stderr logs containing local absolute paths.

Before pushing, run:

```bash
python3 -m pytest 03_agentic-workspace-trojan-ja/experiment/tests
rg -n "(API_KEY|TOKEN=|sk-[A-Za-z0-9]{20,}|password=|credential|Authorization:|Bearer [A-Za-z0-9]{20,})" .
find . -name '__pycache__' -o -name '*.pyc'
```
